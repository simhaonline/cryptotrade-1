
@{
    ViewBag.Title = "User";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}
<div class="row">
    <!-- Column -->
    <div class="col-lg-4 col-xlg-3 col-md-5">
        @Html.Action("UserProfile", "Admin", new { userid = ViewBag.userid })
        @Html.Action("ChangePicture", "Admin", new { userid = ViewBag.userid })
    </div>
    <!-- Column -->
    <!-- Column -->
    <div class="col-lg-8 col-xlg-9 col-md-7">

        @Html.Action("UserDetail", "Admin", new { userid = ViewBag.userid })
        @Html.Action("UpdateBalance", "Admin", new { userid = ViewBag.userid })
    </div>
    <!-- Column -->
</div>

@section css{
    <link href="~/css/croppie.css" rel="stylesheet" />
    <style>
        #upload-demo {
            width: 300px;
            height: 300px;
            padding-bottom: 25px;
        }
    </style>
}
@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
    <script src="~/js/croppie.min.js"></script>
    <script src="~/js/imageUpload.js"></script>

}
